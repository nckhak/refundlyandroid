<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Nødvendige styles for at omgå preferenceFragmentCombat default styles -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="android:listSeparatorTextViewStyle">@style/AppTheme.ListSeperatorTextView</item>
    </style>

    <!-- Formodentlig ikke i brug længere - blev lavet før overgang til fragmenter -->
    <style name="PreferenceStyle">
        <item name="android:textColor">@color/colorDarkGreen</item>
        <item name="android:textColorSecondary">@color/colorDarkGreen</item>
        <item name="android:colorAccent">@color/colorDarkGreen</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <!-- OBS: Måske ikke i brug længere - en del af toolbareventyret -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Nødvendige styles for at omgå preferenceFragmentCombat default styles -->
    <style name="AppTheme.ListSeperatorTextView">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>


</resources>
